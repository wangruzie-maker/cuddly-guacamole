# 社媒选题与创作工具 · 同事使用说明

## 推荐首次流程（完整压缩包）

1. 解压压缩包  
2. **先双击 `检测并安装能力.command`** → 查看能力清单 → 输入 **Y** 安装缺失项  
3. 再双击 **`打开小红书提取工具.command`** 启动  
4. 浏览器打开 `http://127.0.0.1:8765`

以后日常：只需双击「打开小红书提取工具」。核心能力已齐时**不会再弹安装提示**。

> 汇报演示：双击 `体验版.command`（预置样例，不必装 Whisper）。

---

## 两种部署方式

### 方式 A：完整压缩包（发给同事，推荐）

| | |
|--|--|
| 获取 | 负责人用 `./pack_full.sh` 打出 zip，约 400MB+ |
| 内置 | 代码 + `redbook-skills` + Whisper 模型 |
| 同事要做 | 解压 →「检测并安装能力」同意装 Python 依赖 → 打开工具 |
| 仍需自备 | Python 3.10+、Chrome；AI 填 TokenHub Key；采集时扫码登录小红书 |

### 方式 B：从 GitHub 拉取

| | |
|--|--|
| 获取 | `git clone https://github.com/wangruzie-maker/cuddly-guacamole.git` |
| 内置 | 代码 + `vendor/redbook-skills`（**不含** Whisper 大模型，避免撑爆仓库） |
| 你要做 | `./ensure_capabilities.sh --manual` 同意安装依赖并下载 Whisper → `./open_app.sh` |
| 仍需自备 | 同上（API / 扫码 / 可选 Playwright） |

两种方式启动后能力一致；压缩包只是少了「首次下 Whisper」这一步。

---

## 手动 vs 自动安装

| 入口 | 行为 |
|------|------|
| `检测并安装能力.command` | **手动**：打印 ✅/❌ 清单，再问是否安装 |
| `打开小红书提取工具.command` | **自动**：能力齐全 → 静默启动；仍缺核心能力 → 才询问安装 |

---

## API（自己填）

仅 **AI 选题 / 文案** 需要 TokenHub：

- 网页设置里填 Key，或复制 `.env.example` 为 `.env` 写入 `DEEPSEEK_API_KEY=...`

| 功能 | 要 API？ |
|------|----------|
| 采集、提取、语料、追踪、转写、OCR | 否 |
| AI 选题 / 文案 | **是** |
| 小红书采集 | 否（内置 redbook-skills，工具内扫码） |
| 视频号关键词发现 | 否（可选装 Playwright） |

---

## 本机前提

- macOS + **Python 3.10+**（`python3 --version`）
- 安装依赖时需联网

## 常见问题

**双击没反应？** 右键 → 打开；或：

```bash
cd 解压后的文件夹
chmod +x *.command *.sh
./ensure_capabilities.sh --manual
./open_app.sh
```

**转写/OCR 不可用？** 再跑一次「检测并安装能力」。
