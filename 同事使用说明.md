# 社媒内容提取工具 · 同事使用说明

## 最快体验（汇报演示）

1. 解压收到的 **`社媒内容提取-体验版-v1.7.0.zip`**
2. **Mac**：双击 **`体验版.command`**
3. 浏览器自动打开，可切换 **小红书 / 视频号** 查看预置样例（含口播脚本、点赞等）

> 体验版 **无需** 下载 Whisper、无需登录小红书，打开即可展示。

---

## 正式在自己电脑上用（两条命令）

```bash
chmod +x install.sh open_app.sh   # 首次解压后
./install.sh      # ① 装依赖 + 下载 Whisper small（国内自动走镜像，约 5–10 分钟）
./open_app.sh     # ② 以后每次启动
```

**可以正常用的范围（装好上述即可）：**
- 粘贴小红书 / 视频号链接提取
- 视频口播转写（Whisper small）
- 图片 OCR、导出 Excel

**还需要额外配置的功能：**
- 小红书关键词 / 账号发现 → 需 CDP 登录小红书
- 视频号关键词发现 → 需 `./setup_playwright.sh`

---

## 体验版 vs 正式版

| | 体验版 `./open_demo.sh` | 正式版 `./install.sh` + `./open_app.sh` |
|--|--|--|
| 演示数据 | 内置，打开即看 | 从空列表开始 |
| Whisper | 不需要 | **需要**（install 会下） |
| 真实链接提取 | 可试 | 完整支持 |

---

## 打包体验版（负责人）

在本项目目录执行：

```bash
chmod +x pack_demo.sh open_demo.sh install.sh demo/load_sample_data.sh
./pack_demo.sh
```

生成 `dist/社媒内容提取-体验版-v1.7.0.zip`，直接发给同事。

---

## 常见问题

**Q：体验版和正式版区别？**  
体验版预置 4 条演示数据，打开就能汇报；正式版从空列表开始，可粘贴真实链接。

**Q：转写一直等待？**  
先运行 `./install.sh` 下载 Whisper；国内网络慢时用镜像：
`HF_ENDPOINT=https://hf-mirror.com ./setup_whisper.sh`

**Q：Windows 怎么启动？**  
解压后在文件夹地址栏输入 `cmd`，执行：
`python -m pip install -r requirements.txt` 然后 `python open_demo.sh`（或安装 Git Bash 后 `./open_demo.sh`）。
