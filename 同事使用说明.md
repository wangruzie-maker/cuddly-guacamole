# 社媒选题与创作工具 · 同事使用说明（通用 Mac）

## 推荐：离线即用版（v1.42+）

解压 **`社媒选题与创作工具-离线即用版-vX.Y.Z.zip`** 后：

1. 右键 **`打开小红书提取工具.command`** → 打开  
2. 浏览器打开即可用  

已内置：Python 3.12、依赖、Whisper 模型、小红书采集库。  
**不用再点「检测并安装能力」**，也不用联网下载。  
AI Key 找负责人要，在网页里填。本机仍需安装 **Google Chrome**。

---

## 通用设备：第一次怎么开（完整压缩包，非离线版）

### 0）本机需要有的东西
- macOS + **Google Chrome**（必须；Safari / 仅系统浏览器不行，登录小红书靠 Chrome 调试端口 9222）
- **Python 3.12**（推荐官方安装包；系统自带 3.9 不够用）

没有 Python 时：先双击 **`准备Python环境.command`**（会下载官方 3.12 安装包，一路下一步即可）。
没有 Chrome 时：先安装 https://www.google.com/chrome/

### 1）装工具能力
1. 解压收到的 zip  
2. 双击 **`检测并安装能力.command`** → 看清单 → 输入 **Y**  
   （依赖装进项目自己的 `.venv`，不改坏你电脑其它 Python）  
3. 双击 **`打开小红书提取工具.command`**  
4. 浏览器打开后即可用

以后日常：只点「打开小红书提取工具」。能力齐了不会反复弹窗。

> 只做汇报演示：点 `体验版.command`。

---

## 两种交付方式

| | **完整压缩包（推荐发给同事）** | **GitHub 拉取（开发/备份）** |
|--|--|--|
| 拿到什么 | zip，约 400MB | `git clone` 仓库 |
| 已带 | 代码 + 小红书采集库 + Whisper 模型 | 代码 + 采集库（**无** Whisper，首次要下） |
| 同事要做 | 准备 Python → 检测安装 → 打开 | 同上，且可能多等 Whisper 下载 |
| 每人自备 | Chrome；AI 填 TokenHub Key；采集时扫码 | 同左 |

---

## 脚本入口（给同事看的就这三个）

| 双击 | 作用 |
|------|------|
| `准备Python环境.command` | 没有 Python 3.12 时先点这个 |
| `检测并安装能力.command` | 看缺什么，同意后装进 `.venv` |
| `打开小红书提取工具.command` | 日常启动 |

---

## API 要不要填

| 功能 | 要 API？ |
|------|----------|
| 采集、提取、语料、追踪、转写、OCR | 否 |
| AI 选题 / 文案 | **要**，网页填 TokenHub Key |
| 小红书采集 | 否（扫码登录） |
| 视频号关键词发现 | 否（可选装 Playwright） |

---

## 常见问题（通用机）

**只有系统 Python / 报版本太旧？**  
双击 `准备Python环境.command`，选官方安装包。

**报 externally-managed-environment / encodings / prefix=/install？**  
不要手动 `pip3 install`。删掉坏环境后重来：

```bash
cd 解压后的文件夹
rm -rf .venv
./ensure_capabilities.sh --manual
```

新版本会自动避开损坏的 uv Python。

**OCR 装不上？**  
用 Python **3.12**（不要只用 3.13/3.14），再 `rm -rf .venv` 后重装能力。

**登录小红书失败 / 无法启动 Chrome CDP（port=9222）？**  
即使本机已装 Google Chrome，日常 Chrome **正在用** 时，调试端口 9222 也常起不来。  
1. 菜单栏 **Chrome → 完全退出**（不是只关窗口）  
2. 再用 **v1.41.2+** 点「登录小红书」（会强制新开一个调试 Chrome）  
3. 「检测并安装能力」里确认 Chrome 一项为 OK  
4. 仍失败时把弹窗里的完整报错发给负责人  

**双击没反应？** 右键 → 打开；或终端：

```bash
cd 解压后的文件夹
chmod +x *.command *.sh
./准备Python环境.command   # 若需要
./ensure_capabilities.sh --manual
./open_app.sh
```
